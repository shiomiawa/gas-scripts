# CLAUDE.md

このファイルは、このリポジトリで作業する Claude Code 向けのガイドです。

## プロジェクト概要

Google Apps Script (GAS) のスクリプトを管理するプロジェクト。

## 言語・コミュニケーション

- ユーザーへの返答、コミットメッセージ、コード内コメントは日本語で書く。
- 識別子(変数名・関数名)は英語で、既存コードの命名規則に合わせる。

## コーディング規約

- 既存コードのスタイル(インデント、命名、コメント密度)に合わせる。
- 認証情報・APIキー・スプレッドシートIDなどの機密値はコードに直書きせず、スクリプトプロパティ (`PropertiesService`) を使う。

## Git運用ルール

- **コードを変更するたびに、変更をコミットして GitHub にプッシュする。** 複数の変更をまとめて後回しにしない。
- 1つの変更(機能追加・修正・リファクタリングなど)につき1コミットを基本とし、コミットメッセージには何をなぜ変更したかを簡潔に書く。
- プッシュ前に `git status` と `git diff` で変更内容を確認し、意図しないファイルや機密情報(認証情報、`.clasprc.json` など)が含まれていないことを確かめる。
- `main` ブランチへの強制プッシュ (`git push --force`) や履歴の書き換えはユーザーの明示的な指示がない限り行わない。
- プッシュに失敗した場合(リモート未設定、認証エラー、コンフリクトなど)は、黙って先へ進まずユーザーに報告する。
